// Create a variable 'multiply' =62
var num1=31; 
var num2= 2;  
var multiply = num1 * num2; // 31 * 2 = 62
console.log(multiply)

// Create a variable 'random' that generates a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Generates random integers from 1 to 100

// Create a variable 'mod' that calculates the remainder when num3 is divided by num4; remainder is 4
const num3 = 28; // Example value
const num4 = 6;  // Example value
const mod = num3 % num4; // 28 % 6 = 4

// Create a variable 'max' that finds the highest number in a set; returns 20
const max = Math.max(5, 10, 20); // Finds the maximum value, which is 20                              